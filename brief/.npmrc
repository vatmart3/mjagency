# @react-three/fiber déclare des pairs optionnels Expo (react-native).
# npm tente de les résoudre et bloque l'installation ; cette ligne lui dit
# de ne pas s'en mêler. Vercel lit ce fichier comme npm en local.
legacy-peer-deps=true
